@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Dados Pessoais";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3>@ViewData["Title"]</h3>

<div class="row">
    <div class="col-md-6">
        <p>Essa página permite que você faça o download dos dados da sua conta em um arquivo externo.</p>
        <p>
            <strong>Deletar a sua conta irá permanentemente excluir todos os seus dados. Esse processo não pode ser desfeito.</strong>
        </p>

        <div class="d-flex gap-2">

            <div class="flex-fill">
                <form id="download-data" asp-page="DownloadPersonalData" method="post" class="h-100">
                    <button class="btn btn-primary w-100 h-100" type="submit">Baixar Dados</button>
                </form>
            </div>

            <div class="flex-fill">
                <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger w-100">Deletar Conta</a>
            </div>

        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
